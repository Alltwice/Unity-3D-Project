# 修改与交付规则

## 项目文档

涉及架构、职责边界、依赖方向或跨模块逻辑时，优先阅读 `Docs/Architecture.md`；涉及具体文件位置、类型职责、调用方或修改范围时，优先阅读 `Docs/CodeMap.md`。仅在任务相关时读取对应文档，文档不能替代对实际源码的检查；若文档与源码不一致，以当前源码为准。

涉及代码或资源修改的任务结束前，应检查本次修改是否改变了架构、职责边界、关键逻辑流或文件/类型映射；如有变化，同步更新对应文档。没有相关变化时不要为了“更新文档”而制造无意义改动。

更新 `Docs/Architecture.md` 或 `Docs/CodeMap.md` 时，只更新当前实现的事实性内容，如架构、职责、依赖关系、数据流、文件位置和类型映射；不得在这两份文档中新增规范、约束、要求、建议、操作指引等，若发现相关表述及时修正删除即可。

## 防御性编程

编写或修改代码时，避免为了暂时消除报错而机械加入大量判空、日志、提前返回或运行时兜底。

遇到问题时，应优先分析根本原因，并从依赖关系、初始化流程、生命周期、配置或职责划分等方面解决，而不是仅隐藏错误表现。

必要的防御性处理可以保留，但应有明确目的。不要用防御性代码掩盖错误配置或不合理的项目结构，也不要在清理防御性代码时误删正常的业务判断。

## 代码格式

代码格式应整体保持紧凑、连续，避免为了视觉分隔而在类或函数内部频繁插入空行。

对于较长的函数调用、条件表达式或语句，不要仅因行长度而机械换行或增加多层缩进；在不影响可读性的前提下，优先保持单行。

同时应尽量保持现有项目的代码风格，避免产生与当前修改无关的格式调整。

## 修饰符使用

编写类、结构体、字段或变量时，不要默认添加 `sealed`、`readonly` 等限制性修饰符。

只有在当前设计确实需要禁止继承、禁止重新赋值，或需要明确表达稳定不变量时，才使用对应修饰符。不要仅因为“更安全”“更严格”或“未来可能有用”而机械添加，也不要在修改无关代码时顺手增加这些限制。

## 注释维护

修改已有函数、参数、字段、枚举、结构体或类的职责与语义时，应同步检查并调整对应注释，使其准确反映当前实现。

如果原有注释已经因行为或含义变化而失效，应修改或删除，不要原封不动地保留、复制或照搬旧注释。注释应优先描述当前职责、约束和设计原因，避免留下与实际代码不一致的说明。

新增注释时尽量保持简洁，并与项目现有风格一致。AI 新增的普通注释通常不需要在末尾额外添加句号，除非上下文或既有格式确有需要。

## 测试代码

用户未明确要求时，默认不得新增测试文件或测试用例。只有纯算法、跨帧生命周期、关键不变量、已复现回归或关键资产合法性需要自动验证时，才可以提出新增测试；新增前应先说明 Given / When / Then、测试失败代表的外部行为，以及现有测试为何不能覆盖。

测试应验证稳定的输入、输出和行为契约，不得通过反射检查私有字段或私有方法，也不得锁定内部类拆分、精确资产数量、文件名或 UI 节点，除非用户明确将其定义为稳定契约。不得仅为测试方便扩大生产代码的公开 API。

既有测试失败时，应先判断是生产行为缺陷还是测试契约已经过期，不得为了让测试通过而机械修改生产代码或断言。用户明确改变行为契约时，可以同步修改或删除对应旧测试，但交付报告应说明原规则、当前规则和修改原因。

验证结果应明确区分静态检查、`dotnet build` 和 Unity Test Runner 的实际执行结果，不得把编译成功表述为 Unity 测试通过。动画观感、卡顿、滑步和输入手感属于人工验证范围，不能以单元测试代替。

## 完成报告

任务结束后，建议简要说明：

1. 修改了哪些文件。
2. 每个文件修改的具体行号或行号范围。，
3. 修改了哪一部分内容。
4. 被修改部分原本或当前负责的功能职责。
5. 如涉及防御性代码，简要说明保留、删除或调整的原因。

对于涉及逻辑、职责或调用关系变化的任务，可进一步概括修改前后的主要逻辑流向，重点指出数据、状态或控制权从哪里产生、经过哪些模块以及最终由谁消费。

同时提供一份可直接用于人工检查的 Code Review 引导。
Code Review 应根据实际依赖和调用关系给出：
- 推荐的 Review 起点、顺序和终点。
- 每个主要文件或方法值得检查的具体位置与内容。
- 关键的数据流、状态流或调用链。
- 重要边界条件、潜在影响范围，以及不需要继续追踪的无关路径。
复杂逻辑可以用简短的调用链或状态流表示，如果某模块尚未接入生产调用链，也应明确指出，避免 Review 沿错误路径继续展开，目标是让 Review 者能够按照报告直接打开代码逐项检查，而不是重新分析一次整个实现。
